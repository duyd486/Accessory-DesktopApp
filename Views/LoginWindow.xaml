<Window x:Class="Accessory_DesktopApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Accessory_DesktopApp.Views" xmlns:viewmodels="clr-namespace:Accessory_DesktopApp.ViewModels" 
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5"
        Title="LoginWindow" Height="600" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <!---Panel gradient bên trái-->
        <Border Grid.Column="0" Margin="20" CornerRadius="20" ClipToBounds="True">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0EA5E9" Offset="0.0"/>
                    <GradientStop Color="#2563EB" Offset="0.5"/>
                    <GradientStop Color="#1E3A8A" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <StackPanel VerticalAlignment="Bottom" Margin="30">
                <TextBlock Text="Accessory Desktop App" Foreground="White" FontSize="14" Opacity="0.8" Margin="10 0 0 5"/>
                <TextBlock Text="Truy cập vào trung tâm cá nhân để làm việc hiệu quả và tăng năng suất hơn" 
                   Foreground="White" FontSize="20" FontWeight="Bold" 
                   TextWrapping="Wrap" Margin="10 0 0 0"/>
            </StackPanel>
        </Border>

        <!--Title Đăng nhập-->
        <Grid Grid.Column="1">

            <StackPanel Margin="0 80 0 0" HorizontalAlignment="Center">

                <TextBlock Text="Đăng nhập tài khoản" FontWeight="Bold" FontSize="36" Foreground="#00072d" HorizontalAlignment="Center"/>
                <TextBlock Text="Chào mừng bạn đã quay trở lại! Vui lòng đăng nhập để tiếp tục" FontWeight="Light" FontSize="14" Foreground="SlateGray" Margin="0 10 0 30"/>

            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!--Ô đăng nhập (phần này của m t giữ nguyên ko thay đổi gì lứm)-->

                <Grid.Resources>
                    <Style TargetType="PasswordBox">
                        <Setter Property="Background" Value="WhiteSmoke"/>
                        <Setter Property="Foreground" Value="#091540"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Light"/>
                        <Setter Property="BorderBrush" Value="LightGray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">

                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>


                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="WhiteSmoke"/>
                        <Setter Property="Foreground" Value="#091540"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Light"/>
                        <Setter Property="BorderBrush" Value="LightGray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>

                <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                    <TextBlock Text="Địa chỉ Email" Foreground="#00072d" FontSize="15" FontWeight="Bold" />
                    <TextBox Text="{Binding EmailText}" Width="400" Height="40" FontSize="20" Margin="0 10 0 0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Mật khẩu" Margin="0 15 0 0" Foreground="#00072d" FontSize="15" FontWeight="Bold" />
                    <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Width="400" Height="40" FontSize="20" Margin="0 10 0 0" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1"  VerticalAlignment="Center" HorizontalAlignment="Center">

                    <Button Margin="0 30 0 0" Width="400" Height="45" FontSize="15" Content="Đăng nhập" Foreground="AntiqueWhite" FontWeight="SemiBold" Cursor="Hand" Background="#1D4ED8" Command="{Binding LoginCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="10"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="DarkGray"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#DDDDDD"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
