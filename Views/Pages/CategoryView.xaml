<UserControl x:Class="Accessory_DesktopApp.Views.Pages.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accessory_DesktopApp.Views.Pages"
             xmlns:vm="clr-namespace:Accessory_DesktopApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="820">
    
    <UserControl.DataContext>
        <vm:CategoryViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Danh sách danh mục" FontSize="22" FontWeight="Bold" />
                <TextBlock Text="Quản lý danh mục chứa sản phẩm" Foreground="#7A7A7A" Margin="0,6,0,0" />
            </StackPanel>

            <Button
                Style="{StaticResource PrimaryButtonStyle}"
                Background="#1E3A8A"
                Width="80"
                Height="40"
                Grid.Column="1"
                Content="Làm mới"
                Padding="12,6"
                Margin="12,0,0,0"
                Command="{Binding RefreshCommand}" />
        </Grid>

        <Separator Grid.Row="1" Margin="24,-144,24,0" />

        <!-- Search -->
        <Border
            Style="{StaticResource StatCardStyle}"
            Grid.Row="1"
            Padding="0"
            Margin="24,16,24,20">

            <Grid Margin="16">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">

                    <TextBlock
                        Margin="0,0,0,6"
                        FontWeight="SemiBold"
                        Text="Tìm kiếm"/>

                    <TextBox
                        Height="38"
                        VerticalContentAlignment="Center"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                </StackPanel>

                <Button
                    Background="#1E3A8A"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Grid.Column="1"
                    Width="100"
                    Height="38"
                    Margin="12,24,0,0"
                    Command="{Binding SearchCommand}"
                    Content="Tìm kiếm"/>

                <Button
                    Background="#1E3A8A"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Grid.Column="2"
                    Width="100"
                    Height="38"
                    Margin="12,24,0,0"
                    Command="{Binding OpenAddCommand}"
                    Content="+ Thêm"/>

            </Grid>

        </Border>

        <!-- Tree -->
        <Border
            Style="{StaticResource StatCardStyle}"
            Grid.Row="2"
            Margin="24,0,24,24"
            Padding="10"
            CornerRadius="10">

            <TreeView
                ItemsSource="{Binding FilteredCategories}"
                BorderThickness="0"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <TreeView.Resources>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Margin" Value="0 4"/>
                        <Setter Property="Padding" Value="4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </TreeView.Resources>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding children}">
                        <Border Background="#F9FAFB"
                                CornerRadius="8"
                                Padding="0 10 0 10"
                                Margin="0 4"
                                HorizontalAlignment="Stretch">

                            <DockPanel
                                LastChildFill="True"
                                HorizontalAlignment="Stretch"
                                Width="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=ActualWidth}">

                                <StackPanel
                                    DockPanel.Dock="Right"
                                    Margin="0 0 60 0"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">

                                    <Button
                                        Background="#FFD54F"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Width="60"
                                        Height="32"
                                        Margin="0,0,6,0"
                                        Command="{Binding DataContext.OpenEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="Sửa"/>

                                    <Button
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Width="60"
                                        Height="32"
                                        Background="#B84A4A"
                                        Foreground="White"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding id}"
                                        Content="Xóa"/>

                                </StackPanel>

                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding id}"
                                               Width="40"
                                               Foreground="#9CA3AF"
                                               VerticalAlignment="Center"/>

                                    <Border Width="34" Height="34"
                                            CornerRadius="6"
                                            Background="#E5E7EB"
                                            Margin="0 0 10 0"
                                            ClipToBounds="True">

                                        <Image
                                            Stretch="UniformToFill"
                                            Source="{Binding thumbnail_url, Converter={StaticResource NoCacheImageConverter}}"/>
                                    </Border>

                                    <TextBlock Text="{Binding title}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               Foreground="#111827"/>
                                </StackPanel>

                            </DockPanel>

                        </Border>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>

            </TreeView>

        </Border>

        <!-- Dialog (Add/Edit) -->
        <Border
    Grid.RowSpan="3"
    Background="#80000000"
    Visibility="{Binding IsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}">

            <Border
        Width="520"
        Background="White"
        CornerRadius="12"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">

                <Border.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="WhiteSmoke"/>
                        <Setter Property="Foreground" Value="#091540"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="BorderBrush" Value="LightGray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Height" Value="38"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="ComboBox">
                        <Setter Property="Background" Value="WhiteSmoke"/>
                        <Setter Property="Foreground" Value="#091540"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="BorderBrush" Value="LightGray"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Height" Value="38"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBox">
                                    <Grid>
                                        <ToggleButton
                                    x:Name="PART_ToggleButton"
                                    IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="5">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="30"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                        Grid.Column="1"
                                                        Text="▾"
                                                        FontSize="11"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center"
                                                        Foreground="#091540"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="BorderBrush" Value="Gray"/>
                                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>

                                        <ContentPresenter
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"
                                    IsHitTestVisible="False"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                                        <Popup
                                    x:Name="PART_Popup"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    AllowsTransparency="True"
                                    Placement="Bottom"
                                    PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                            <Border
                                        Background="White"
                                        BorderBrush="LightGray"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                                <ScrollViewer>
                                                    <ItemsPresenter/>
                                                </ScrollViewer>
                                            </Border>
                                        </Popup>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="Button">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Border.Resources>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="28">

                        <TextBlock
                    Margin="0,0,0,20"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#00072D"
                    Text="{Binding DialogTitle}"/>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Ảnh -->
                            <StackPanel Grid.Column="0">
                                <Border
                            Width="140"
                            Height="140"
                            CornerRadius="8"
                            BorderBrush="#DDD"
                            BorderThickness="1"
                            Background="WhiteSmoke">
                                    <Image
                                Stretch="UniformToFill"
                                Source="{Binding ThumbnailPreview}"/>
                                </Border>

                                <Button
                            Margin="0,10,0,0"
                            Height="34"
                            Background="#1E3A8A"
                            Foreground="White"
                            Content="Chọn ảnh"
                            Command="{Binding PickImageCommand}"/>
                            </StackPanel>

                            <!-- Form -->
                            <StackPanel Grid.Column="2">

                                <TextBlock Text="Tên danh mục" FontWeight="SemiBold" Foreground="#00072D" Margin="0,0,0,6"/>
                                <TextBox Margin="0,0,0,14" Text="{Binding CategoryForm.title}"/>

                                <CheckBox
                            Margin="0,0,0,14"
                            FontSize="13"
                            Foreground="#091540"
                            Content="Đặt làm danh mục cha"
                            IsChecked="{Binding IsParentCategory}"/>

                                <TextBlock Text="Danh mục cha" FontWeight="SemiBold" Foreground="#00072D" Margin="0,0,0,6"/>
                                <ComboBox
                            ItemsSource="{Binding ParentCandidates}"
                            DisplayMemberPath="title"
                            SelectedValuePath="id"
                            SelectedValue="{Binding CategoryForm.parent_id}"
                            IsEnabled="{Binding IsChildCategory}"/>

                            </StackPanel>
                        </Grid>

                        <StackPanel
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

                            <Button
                        Width="100" Height="40"
                        Margin="0,0,10,0"
                        Background="#F1F5F9"
                        Foreground="#374151"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        Command="{Binding CloseDialogCommand}"
                        Content="Hủy"/>

                            <Button
                        Width="100" Height="40"
                        Background="#2563EB"
                        Foreground="White"
                        Command="{Binding SubmitCategoryCommand}"
                        Content="Lưu"/>

                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Border>
    </Grid>
</UserControl>